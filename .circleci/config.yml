version: 2.1

jobs:
  build:
    working_directory: ~/clean_py
    docker:
      - image: cimg/python:3.10.4
    steps:
      - checkout
      - run: pip install tox
      # **failing here
      - run: cd ./project && tox
